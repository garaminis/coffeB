<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.shopping.MemberDAO">
<select id="list" resultType="com.study.shopping.MemberDTO">
    SELECT id, user_id, password, name, mobile, mail, birth, gender, join_day, out_day, zipcode, adress, member_rank
    FROM member0
</select>
</mapper>